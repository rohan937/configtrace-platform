// Orphan layout file — no page.tsx lives here.
// All /dashboard routing is handled by src/app/(app)/dashboard/page.tsx.
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
