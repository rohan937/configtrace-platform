# Development and testing dependencies — not installed in the production image.
# Install with: pip install -r requirements-dev.txt
#
# These complement requirements.txt; install both:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==8.3.3
respx==0.21.1
pytest-asyncio==0.24.0
