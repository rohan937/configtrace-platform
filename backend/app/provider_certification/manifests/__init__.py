"""Pilot provider certification manifests.

Message 1 adopted Sentry and Snowflake — the newest, most complete
eight-message launch arcs, covering both REST and SQL API
authentication/collection patterns with different completeness/
false-removal models.

Message 2 adds Okta (REST identity provider, API-token/domain auth) and
Microsoft Entra ID (Microsoft Graph identity provider, tenant/client-secret
auth), proving the framework generalizes across four distinct
authentication/API shapes. Do not add another provider's manifest without
an explicit later message authorizing it.
"""

from __future__ import annotations
